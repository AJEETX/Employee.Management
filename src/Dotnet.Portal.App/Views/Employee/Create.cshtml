@model Dotnet.Portal.App.ViewsModels.EmployeeViewModel

@{
    ViewData["Title"] = "Create new employee";
}

<h3 class="ui header">Create new employee</h3>

<div class="ui divider"></div>
<partial name="/Views/Components/_ValidationSummary.cshtml" />

<form asp-action="Create" class="ui form segment">
    @{ await Html.RenderPartialAsync("_MemberFormPartial.cshtml"); }
    <a class="ui button" asp-action="Index">
        <i class="arrow left icon"></i>
        Back
    </a>
    <button type="submit" class="ui submit primary button">
        <i class="check icon"></i>
        Create
    </button>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
